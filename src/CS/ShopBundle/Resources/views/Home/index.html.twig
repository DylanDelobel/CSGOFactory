{% extends 'ShopBundle::layout.html.twig' %}

{% block stylesheets %}
	<link href="{{ asset('css/shop/homepage.css') }}" rel="stylesheet">
{% endblock %}

	{% block content %}

    <div class="container">

        {% if listCollection is empty %}
        <div class="row">
            <div class="col-lg-12">
                <h3>Collection inexistante</h3>
            </div>
        </div>
        {% else %}
            {% for Collection in listCollection %}
                <div class="row">
                    <div class="col-lg-12">
                        <h3>{{ Collection.name }}</h3>
                    </div>
                </div>
        
                <!-- /.row -->


                <!-- Page Features -->
                <div class="row text-center">
                {% set i = loop.index - 1 %}
                    {% for Weapons in listWeapons[i] %}

                    <div class="col-md-3 col-sm-6 hero-feature">
                        <div class="thumbnail">
                            <img class="imageShop" src="{{ Weapons.image.path }}" alt="">
                            <div class="caption">
                                <h3><a href="#">{{ Weapons.name }}</a></h3>
                                <p>Quality : {{ Weapons.quality }}</p>
                                <p>
                                    <a href="#" class="btn btn-primary">Buy Now!</a> <a href="#" class="btn btn-default">More Info</a>
                                </p>
                            </div>
                        </div>
                    </div>

                    {% endfor %}


                </div>
                
                
                <!-- /.row -->

            {% endfor %}
        {% endif %}

        <hr>

        
    </div>
{% endblock %}
